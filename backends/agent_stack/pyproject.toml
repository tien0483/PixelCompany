[project]
name = "agent-stack-sandbox"
version = "0.1.0"
description = "Local switchboard + proxy router for the sandboxed agent stack"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "python-multipart>=0.0.9",
    "headroom-ai>=0.34.0",
    "h2>=4.4.1",
]

[tool.uv]
package = false
