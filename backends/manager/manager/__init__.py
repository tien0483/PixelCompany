"""
PixelOffice Manager — multi-account manager + skills suite for Claude Code.

Installs a curated suite of skills, commands, agents, and behavioral rules
into Claude Code, and runs a web dashboard + tray service for managing
multiple Claude/Codex accounts with live usage tracking and auto-swap.

  cd backends/manager && uv sync && manager install
"""

__version__ = "0.88.0"

__all__ = [
    "__version__",
]
