# Assets moved to `.agent/manager/`

All Manager data now lives in the PixelOffice repo:

| Path | Contents |
|------|----------|
| `.agent/manager/data/` | Catalog — skills, agents, commands, rules, packs |
| `.agent/manager/runtime/` | Hooks, web UI, lenses, guardrails, git-hooks, templates |

The Python package (`backends/jacked`) is code-only for PixelOffice.

See `.agent/manager/UNFOLD.md`.
