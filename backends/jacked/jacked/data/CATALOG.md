# Catalog moved

Skills, agents, commands, rules, and `packs.json` live in the PixelOffice repo at:

`.agent/jacked/data/`

This package directory keeps **runtime assets** only: hooks, web UI, lenses, guardrails,
git-hooks, templates, and `.claude-plugin/`.

See `.agent/jacked/UNFOLD.md` for the plan to move the remaining runtime assets out of
the package as well.
