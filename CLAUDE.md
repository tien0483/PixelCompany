# PixelOffice-v2

## Agent stack

Installed at `backends/agent_stack`, symlinked into every task worktree. Two
tools are available without any setup:

**`understand-chat` — codebase questions.** When a question spans three or more
files ("how does X work", "what calls Y", "where does Z get set"), check for
`.ua/knowledge-graph.json` first:

- Graph present → answer via the `understand-chat` skill instead of opening files
  one by one.
- Graph absent → answer normally, and mention once that `/understand` would build
  the graph. Do **not** run `/understand` unprompted: a full build reads the whole
  repo and is expensive. It also redirects worktree output to the main repo root,
  so one build serves every task.

**`rtk` — token-compressing CLI proxy.** On PATH in every runtime-spawned agent
session. Prefer it for shell commands with bulky output — `rtk git status`,
`rtk tsc`, `rtk lint`, `rtk test`, `rtk grep`. It only helps for *shell* commands:
the Read/Grep/Glob tools never pass through it, so keep using those for file
reads. `rtk gain` shows what it saved.

Proxy routing (Headroom/CCR) is deliberately NOT applied to task agents — see
`backends/runtime/src/stack/stack-process.ts` for why.

**Exception: subagent seats.** A card can pin an API seat that only its *subagents*
bill (Account picker → "Subagents", Claude Code only). Such a task launches with
`ANTHROPIC_BASE_URL` at the switchboard and `CLAUDE_CODE_SUBAGENT_MODEL=ccr-<port>,<model>`,
and never with `ANTHROPIC_API_KEY` — so the parent keeps its own OAuth seat while the
switchboard diverts marker-model requests to a per-seat CCR on 3460+. The user's own
`ENABLE_CCR` router keeps 3456. Every failure degrades to "subagents share the task's
seat" instead of blocking the launch.

## Harness: PixelOffice merge

**Goal:** Unified monorepo (`frontends/pixel_office` + `backends/runtime` + `backends/manager`) with three-pane home and Claude-only Manager OAuth.

**Trigger:** For PixelOffice merge, flatten, three-pane layout, docked office, Manager Accounts/OAuth, or related follow-ups, use the `pixeloffice-merge-orchestrator` skill. Simple questions can be answered directly.

**Change log:**

| Date | Change | Target | Reason |
|------|--------|--------|--------|
| 2026-07-30 | Initial build | All | Three-pane merge harness |
| 2026-07-30 | Flatten stack | All | Remove nested kanban / claude-jacked-master donors |
| 2026-07-30 | Claude-only gate | Runtime + UI | Cline gated at the agent catalog (reversible); Claude Code is the default and only launchable agent |
| 2026-07-30 | Runtime owns Manager | Runtime + scripts | Manager spawned headless by the runtime; `scripts/solo.mjs` serves everything on one URL |
| 2026-07-30 | Multi-account | Manager + Runtime + UI | Account actions/sessions ported; per-task pinning via `CLAUDE_CONFIG_DIR` |
| 2026-07-31 | Neutral runtime home | Runtime | `~/.cline` → `~/.agent`; boards copied forward, legacy worktrees left in place |
| 2026-07-31 | Manager office theme | UI | Manager surface reads as Manager / Seats / Staff / Playbooks / Training / Handbook (labels only) |
| 2026-07-31 | Skills + packs UI | Runtime + UI | Four feature shelves in the sidebar; `/api/packs` bridged for curated skill bundles |
| 2026-07-31 | WSL dev-setup docs | Docs | Run from native Linux fs, not `/mnt/<drive>`; 9p I/O makes `pnpm start` hang forever on `node_modules` resolution |
| 2026-07-31 | Manager venv auto-detect | Runtime | `resolvePythonBinary` prefers `backends/manager/.venv/bin/python` over bare `python3`; fixes silent auth-router ImportError → 405 on OAuth |
| 2026-07-31 | UI dist auto-rebuild hooks | Root + docs | `.githooks/post-merge`/`post-checkout` rebuild `frontends/pixel_office/dist` when it drifts from HEAD; `core.hooksPath` set via `prepare` script |
| 2026-07-31 | No AI attribution in commits/PRs | Docs | User preference — strip Co-Authored-By and Generated-with footers for this repo |
| 2026-08-01 | npm → pnpm migration | Root + workspaces | Per-worktree `npm install` was duplicating full downloads/disk per agent worktree; pnpm's content-addressable store (`~/.pnpm-store`) shares packages across worktrees. `pnpm-workspace.yaml` holds `packages`, `overrides` (single `zod@4.4.3` — see [[pnpm-prepublish-quirk]]), and `allowBuilds`. Root `package.json` keeps its legacy `"workspaces"` field untouched (pnpm ignores it, cosmetic) per user request — edits to any `package.json` are user-denied for Claude, so dependency/manifest changes must go through `pnpm`/`npm` CLI commands, never the Edit tool. |
| 2026-08-01 | post-checkout hook guard | Scripts | `git worktree add` (task worktree creation) fires the `post-checkout` hook **before** the runtime symlinks `node_modules` into the new worktree, so `scripts/rebuild-ui-if-changed.sh` ran `npx vite build` with no deps → `ERR_MODULE_NOT_FOUND: @tailwindcss/vite` → non-zero hook exit aborted worktree creation, leaving a symlink-less shell and every backlog task failing at start. Script now early-exits when `frontends/pixel_office/node_modules` is absent. |
| 2026-08-07 | Immutable task baseRef + push | Runtime + UI | Persist worktree base ref in branch registry at creation; merge/UI use that over editable card metadata. Git view gains "Push to remote". Chain followers surface the root's locked base ref. |
| 2026-08-09 | Agent stack moved in-tree | Backends + scripts + gitignore | `~/agent-stack-sandbox` → `backends/agent_stack`. Half the stack already shipped in-repo (Stack Control dialog + client, `link-stack-skills.mjs`, `solo.mjs` probe), so a fresh clone had UI for a backend it could not install. Runtime now spawns the switchboard headless (`src/stack/stack-process.ts`, mirroring `manager-process.ts`) — but never exports the proxy env for spawned agents: `activate-stack.sh` sets `ANTHROPIC_API_KEY=sk-dummy-key-for-sandbox` and CCR ships no credentials, so inherited routing would fail every task agent with `Authentication failed`. Payload dirs are gitignored *so that* `syncIgnoredPathsIntoWorktree` symlinks them into each task worktree (UA skills + `bin/rtk` per task, no 2 GB copy). venv rebuilt as `.venv` via `uv sync` — console-script shebangs are absolute, so a venv cannot be moved. |
| 2026-08-09 | Subagent seats | Runtime + UI + agent_stack | Cards can pin an API seat that only the session's *subagents* bill, so orchestration-heavy tasks stop burning the main OAuth seat's 5h/7d cap. Claude Code sends `CLAUDE_CODE_SUBAGENT_MODEL` verbatim as the `model` of every subagent request and nothing else — that is the only per-turn signal separating a subagent from its parent, and no other CLI reads it. Launch sets `ANTHROPIC_BASE_URL` (switchboard) + the marker `ccr-<port>,<modelId>`, never `ANTHROPIC_API_KEY`: Claude Code prefers that key over its OAuth credential, which would move the parent off the card's seat too. `server.py` buffers only `POST /v1/messages`, routes marker models to the seat's router with the model rewritten and caller auth stripped, and now only swaps in `STACK_UPSTREAM_ANTHROPIC_API_KEY` when the caller sent no real credential. The vendored CCR routes **by category only** — `"provider,model"` strings log `Unknown model …, using default` and `routing.rules` is ignored — so each seat gets its own router (3460+, clear of the user's 3456) with CCR's shipped codewhisperer/shuaihong providers neutralized, or they win `default`. |

| 2026-08-11 | Stack daemons + proxy env without a sourced shell | Runtime + scripts | Headroom was started *only* by an interactive `source activate-stack.sh`, so every runtime restart lost it: `resolve_route` found 8787 closed, appended `headroom:8787 DOWN (skipped)` and routed direct — degraded, silent, forever. `src/stack/stack-daemon.ts` is now the shared supervisor (skip an already-served port, backoff restart capped at 5 fast failures, `logs/<name>.pid` + `logs/<name>.log` kept as the activator writes them), used by `headroom-process.ts` and by `stack-extra-daemons.ts` for the flag-gated CCR :3456 / DevTools :3001. Flag semantics copy the activator exactly, including the asymmetry: unreadable `stack-flags.json` = ON, key absent from a readable file = OFF. `scripts/solo.mjs` supplies the other half — `ANTHROPIC_BASE_URL` at the switchboard so task agents traverse the chain, `--no-proxy-env` / `STACK_PROXY_ENV=0` to opt out — but **never** `ANTHROPIC_API_KEY`, and it drops an inherited `sk-dummy-key-*`: Claude Code prefers a key over its OAuth credential, so the placeholder would move every session off its card's seat and 401 unless `STACK_UPSTREAM_ANTHROPIC_API_KEY` is set. Base-URL-only means `has_caller_credential()` forwards the session's OAuth bearer untouched (same trick as subagent seats). Cost caveat measured on this repo: sessions run 97–99% prompt-cache hit rate, and headroom rewrites context, so a broken cache prefix turns a 0.1x cache read into a 1.25x write — watch usage after enabling, `--no-proxy-env` is the lever. |

| 2026-08-14 | `spawn-subagent` catalog skill | agent-data | Ported an external `cat:spawn-subagent` skill into `agent-data/catalog/skills/spawn-subagent/SKILL.md`, so the Manager Training shelf toggles it ON/OFF per project (`_get_valid_skill_names` globs that dir — the shelf is dynamic, no code change) and it reaches card Skills pickers through `managerFeatureInventoryIds`. Retargeted to this repo: spawns are budgeted against the card's pinned **Subagents** seat, read from `CLAUDE_CODE_SUBAGENT_MODEL` (`ccr-<port>,<model>`); absent marker = subagents share the task's seat. Teaches the two real caps — the spawn hook's 2 parallel / 3 s gap / **40 000 prompt chars**, which binds first, and the switchboard's 2 concurrent / 120 s queue / ~180 K tokens — plus the `stack_seat_context_overflow` (413) and `stack_seat_busy` (429) refusals. Never set a per-spawn `model:`: the marker string *is* the routing signal, so overriding it moves the turn back onto the parent's OAuth cap. Upstream's per-subagent git worktrees are dropped (the task worktree is already the isolation boundary; more of them multiply what gets purged and re-trip the pre-symlink `post-checkout` hook). The toggle copies **only `SKILL.md`**, so any catalog skill must be a single self-contained file, with `description:` on one unwrapped line for `parseSkillMarkdownMeta`. |
| 2026-08-14 | Merge to base honors the pinned base ref | Runtime + UI | "Merge to base" was landing on whatever branch the home repo had checked out. Three holes, all upstream of the merge itself: `registerActiveBranch` rewrote the registry entry's `baseRef` on every worktree (re)creation, so a purge/restart re-seeded the "immutable" ref from the card; the card's ref was only *visually* locked (`updateTask` wrote `draft.baseRef` unconditionally, and the editor's save fell back to the current-branch default while its field was still being seeded from the server); and nothing rejected a floating ref, so tasks were recorded with `"baseRef": "HEAD"` (seen live in two workspaces) which by definition follows HEAD. Now: the registry base ref is write-once per entry (only worktree delete clears it) and stores `baseCommit`; creation resolves `HEAD`/`@` to the checked-out branch and refuses when the home repo is detached; `getTaskWorkspaceInfo` reports `baseRefLocked` (false when the recorded ref is not a local branch, which re-unlocks the card picker for the legacy `HEAD` rows); an empty draft ref means "keep the card's". The merge no longer needs the base to be someone's HEAD — `runGitMergeBranchInTemporaryWorktree` borrows a throwaway `git worktree add` for the base branch (git 2.34 here, so no `merge-tree --write-tree`), and chain followers pass `worktreeTaskId` so the root's entry is the one consulted. |
| 2026-08-25 | Seats never write a dead Claude Code token | Manager | Every "Auto" (unpinned) card opened on `Login expired · Please run /login`, and re-logging in from Seats changed nothing. Auto inherits jacked's global active seat, and `build_oauth_data` handed out the `cc_*` pair whenever a `cc_refresh_token` existed — *including a pair whose `cc_expires_at` was days past* — betting Claude Code would refresh it. Nothing else could: the active seat is exactly the seat whose CC refresh every path skips (§7.1's single-use-token protection), so account 2 sat on a token expired since 2026-08-22 that `_write_swap_credentials` re-installed globally on every swap. Three changes, all in `backends/manager`: `resolve_oauth_source` never picks an expired CC pair over a live primary (the file is a launch-time snapshot — an expired `accessToken` *is* the login screen); `cc_refresh_block_reason` carves expired pairs out of the live-session skip (nothing left to protect, and the skip is what froze the seat); and `ensure_launchable_credentials` refreshes before every global-store write (auto-swap, Set Active, CLI reconcile). Seats still fall back to the primary access token with `refreshToken: None` — that is deliberate (Claude Code must not consume the usage credential's refresh token), so a seat with no real CC OAuth still needs `/login` once its ~8 h token lapses. The permanent cure is the dashboard's per-seat **CC** re-authorize button; `cc_needs_auth` under-reports that need (`api/routes/auth.py`, it also requires the primary refresh token to be missing), so the Seats pills look healthy while the CC credential is dead. |
| 2026-08-25 | Same-seat login recovery daemon | Runtime + UI | A card that hit `Login expired · Please run /login` was parked and then restarted **on a different seat** by the existing cross-seat failover — the wrong cure for the common case, where the seat's Claude Code OAuth pair is merely stale and `POST /api/auth/accounts/{id}/launch-dir` (`prepare_account_dir`) would refresh it. The auth-failure reporter is now a router: `login-expired-tracker.ts` keeps per-card pop history and grants **one** same-seat attempt, `login-recovery-daemon.ts` spends it (re-prepare the same seat → `stopTaskSession` → relaunch with `--continue` → type `continue`), and a second pop falls through to the untouched failover. Two things had to be fixed to make it work at all: detection only matched the banner form, so a session showing just the status line (`Not logged in · Run /login` — no "please", no trailing "to") was never reported; and Claude has no prompt-ready event to time the keystrokes against, so `postStartInput` infers readiness from a re-armed quiet-gap timer with a max-wait deadline — the deadline is load-bearing, since trust auto-confirm fires once per session and a redrawn trust prompt would otherwise withhold the input forever. See [[typing-into-a-restarted-claude-pty-has-no-ready-signal]] and [[login-expired-is-cured-by-re-preparing-the-same-seat]] in `backends/runtime/AGENTS.md`. |
| 2026-08-27 | Restarts keep the session's permission mode | Runtime | Every automatic relaunch put the card back in **auto**. The mode is only ever a launch flag here (`--permission-mode auto` from the global autonomous setting, `plan` from the card), nothing watches the user cycling it with shift+tab, and each restart path replays the original request verbatim — so crash auto-restart (10 per 5 s), login-expired same-seat recovery, cross-seat failover and manual resume all re-imposed the default. `--continue` carries nothing: Claude Code's own bundle says `--resume does not restore permissionMode — pass --permission-mode <mode> to match`, and `~/.claude.json` stores no per-project mode. `terminal/claude-permission-mode.ts` therefore tail-reads the newest transcript under `<CLAUDE_CONFIG_DIR|~/.claude>/projects/<encoded cwd>` — Claude Code writes a `{"type":"permission-mode",…}` record on every change and stamps each user turn — and `claudeAdapter` prefers that recorded mode over card/global defaults on any resuming launch (explicit `--permission-mode`/bypass args still win; a fresh start is untouched). Bypass is re-entered as `--dangerously-skip-permissions`, since `--permission-mode bypassPermissions` is gated on the bypass dialog. See [[a-relaunched-claude-session-loses-its-permission-mode-unless-the-flag-comes-back]] in `backends/runtime/AGENTS.md`. |
| 2026-08-31 | Agents tab (Flowise agent studio) | Runtime + UI + scripts | PixelOffice had no way to *author* an agent: `RUNTIME_AGENT_CATALOG` is a closed enum backed by an exhaustive `Record<RuntimeAgentId, AgentSessionAdapter>`, and the only extensible sense of "agent" — `.claude/agents/*.md` (Staff) — is read off disk with no create-UI anywhere (`features.py`/`packs.py` expose no `POST`; every Manager shelf is enable/disable of a pre-authored catalog file). Phase 1 adds a 5th home sidebar section next to Plans/Review that frames a forked Flowise canvas. Upstream is **archived** (2026-08-13, no successor, "fork it"), and its `packages/server/src/enterprise` auth is **commercially licensed**, so `backends/flowise` is a submodule pointing at our fork with that dir replaced by single-user stubs and the UI's MUI theme mapped onto the PixelOffice tokens (`?embed=1` hides its chrome). `flowise-process.ts` supervises it on **3010** — clear of upstream's 3000, DevTools 3001 and CCR 3456/3460+ — modeled on `html-process.ts` and borrowing only `nextRestartDelayMs`/`shouldGiveUpRestarting` from `stack-daemon.ts`, never `superviseStackDaemon`. **Its toolchain is not this repo's:** Flowise declares `engines` `node ^24` / `pnpm ^10.26.0` and pnpm 11 enforces both — every install under node 22 dies with `ERR_PNPM_UNSUPPORTED_ENGINE`, and neither the `engine-strict = false` in Flowise's own `.npmrc` nor `--config.engine-strict=false` suppresses it (pnpm 11 moved that setting out of `.npmrc`). Build with `npx pnpm@10.26.0` under a node 24 install, and note that the *spawn* needs it too: `flowise-node.ts` resolves `PIXELOFFICE_FLOWISE_NODE` → the runtime's own node if new enough → the newest qualifying `~/.nvm/versions/node/v*`, because handing the studio `process.execPath` hands it node 22. Also a trap worth remembering generally: a background `pnpm build > log 2>&1; echo $?; tail log` reports **tail's** exit code, so three "successful" builds here were actually engine failures with no artifacts. Gated on the submodule being **present and built** rather than a `stack-flags.json` key, because an unreadable flags file means every flag is ON and that file is gitignored — a flag would auto-start a heavy service on a fresh clone. The frame is **cross-origin** on 3010, not `/api/flowise-proxy`: the existing `*-proxy` handlers buffer to `response.text()` and the WS-upgrade allowlist destroys every path but the runtime's own, so a SPA bundle plus socket streaming cannot ride them; `CORS_ORIGINS`/`IFRAME_ORIGINS` scope the embed instead. The spawn env strips `ANTHROPIC_*` (solo exports the base URL for *task agents*; inheriting it would silently route every Anthropic node in every flow through the switchboard) and redirects all Flowise state into gitignored `backends/flowise/.flowise/`, which `syncIgnoredPathsIntoWorktree` then symlinks so all tasks share one studio DB. **Security posture:** with the enterprise auth stripped there is no login in front of the canvas, and Flowise's Custom Function nodes execute arbitrary code by design — loopback bind plus those two origin headers are the whole boundary, and upstream will never ship another CVE fix. Phase 2 (not yet built) turns a deployed flow into a card-attachable MCP tool: synthetic `flowise-*` entries in `listClaudeMcpInventory` plus a zero-dependency stdio shim over `POST /api/v1/prediction/<id>`, reusing the card's existing `mcpServerIds` picker and `--mcp-config --strict-mcp-config` launch path unchanged. |
| 2026-08-31 | Learning + Understand buttons | Runtime + UI + submodule | Two surfaces the home TopBar was missing, both center-pane views next to Docs. **Understand** rebuilds nothing: `review.openGraphDashboard` already starts one UA viewer per project path on 5273+ and hands back a URL with its access token in the query string, and the viewer is a bare `node:http` server that sets neither `X-Frame-Options` nor a CSP — so the Review tab's `window.open` becomes an iframe and everything server-side is consumed as-is. It probes with `review.getGraphImpact({changedPaths: []})` before spawning anything, because that is the one call that separates "no graph here" from "a graph that failed to load"; starting the viewer first collapses both into "the dashboard did not start". Build/Rebuild streams `/api/review/graph-rebuild` on the Antigravity seat pinned to `gemini-3.7-flash` + `effort: medium` — deliberately *unlike* `review-workspace-view.tsx:209`, which sends no model because agy's ids change between releases; the pin is the user's call, kept in one exported constant (`UNDERSTAND_REBUILD_MODEL`) so reverting is one line. Never auto-built: a full build reads the whole repo. **Learning** frames OpenMAIC (`THU-MAIC/OpenMAIC`, MIT, pinned at `f6cf8fd4`) on **3020**, because github.com sends `X-Frame-Options: deny` and a raw frame at the repo renders blank. `openmaic-process.ts` copies `flowise-process.ts` (borrowing only `nextRestartDelayMs`/`shouldGiveUpRestarting`, never `superviseStackDaemon`), gated on the submodule being present **and** built (`.next/BUILD_ID`) rather than a `stack-flags.json` key — an unreadable flags file means every flag is ON, which would auto-start a heavy service on a fresh clone. It spawns the submodule's own `next` binary under `process.execPath` (its `.nvmrc` says node 22, so no `flowise-node.ts`-style hunt), not `pnpm start`: OpenMAIC pins `packageManager: pnpm@10.28` while this repo runs pnpm 11, which would negotiate that version over the network on every spawn. `-H 127.0.0.1` is explicit because `next start` defaults to 0.0.0.0 and reads only `PORT` from the env, and `ANTHROPIC_*` is stripped so solo's task-agent base URL never routes the classroom's LLM calls through the switchboard. **Two things the plan assumed that turned out false, both worth remembering:** a superproject `.gitignore` cannot ignore anything inside a submodule (`fatal: Pathspec 'backends/flowise/.flowise' is in submodule` — so that Flowise block, and the `syncIgnoredPathsIntoWorktree` claim attached to it, has always been inert; `.git/info/exclude` written from inside is the only lever, which is what `ensureOpenmaicDataDirExcluded` does for `.openmaic/`); and Next.js reads `.env.local` from the process cwd only, so the provider key lives at `backends/openmaic/.env.local` (already covered by upstream's `.env*`) and no data-dir env var is set, since none is documented and an unread var implies a contract upstream does not honour. **Building it is three traps deep, all found the hard way.** (1) `X-Frame-Options: SAMEORIGIN` is baked into `.next/routes-manifest.json` at *build* time by upstream's `next.config.ts` `headers()`, and PixelOffice (:3484) is a different origin from the classroom (:3020) — so a default build renders a blank frame with no error anywhere, browser or server. Upstream's own opt-in is `ALLOWED_FRAME_ANCESTORS`, which drops the header and widens the CSP, but it is read while *building*: setting it in the spawn env is silently ignored, so the supervisor cannot repair a bad build and instead detects one (`isOpenmaicBuiltForEmbedding` greps the manifest) and the Learning tab gets a fourth state — "running, but refuses to be embedded", with an Open-in-a-tab button, because that case works everywhere except the frame. The origin list comes from `resolvePixelOfficeEmbedOrigins`, the same helper Flowise uses, so the two sidecars cannot disagree about what "this PixelOffice" means. (2) Install must run under **pnpm 10.28 + `CI=true`**, exactly as upstream's Dockerfile does (`corepack prepare pnpm@10.28.0`): pnpm 11 fails the install with `ERR_PNPM_IGNORED_BUILDS` *and silently writes an `allowBuilds:` scaffold into upstream's tracked `pnpm-workspace.yaml`, so check `git -C backends/openmaic status` after any pnpm 11 run*; and pnpm 10 then refuses to replace pnpm 11's `node_modules` with `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY` unless `CI=true`. Dependency build scripts stay blocked throughout, which is both upstream's posture and the safer one — no vendor postinstall code runs, and the build does not need them. (3) It **cannot be built inside a task worktree**: Next picks the outermost lockfile as its workspace root, lands on the worktree root, walks into `node_modules -> /home/ubuntu/work/PixelCompany/node_modules` and dies with `Symlink node_modules is invalid, it points out of the filesystem root`. That symlink is the runtime's own infrastructure, so build in the home repo — which is also the only build the runtime can load, since `findOpenmaicRoot()` resolves next to wherever solo started. `next build --webpack` is not an escape hatch: OpenMAIC is Turbopack-only (`instrumentation.ts` imports `fs`). **Security posture:** like the Flowise canvas, the embedded classroom has no login and the loopback bind is the entire boundary — never 0.0.0.0. Center-view state moved to `use-home-center-view.ts`: one `HomeCenterView` enum replacing the boolean pile, because four flags need six pairwise resets and each new surface added quadratically more places to forget. |
| 2026-08-14 | Cline gate reversed (correction) + `~/.cline` is half-live | Docs | The 2026-07-30 "Cline gated at the agent catalog" row above is **stale** and stayed stale long enough to cause data loss. `5b00886` (2026-08-07) uncommented `"cline"` in `RUNTIME_LAUNCH_SUPPORTED_AGENT_IDS` (`backends/runtime/src/core/agent-catalog.ts`); Cline is launch-supported and runs in-process through the SDK, and the only remaining gate is cosmetic — `task-agent-model-picker.tsx` hides it until an API-key seat exists. Consequence on 2026-08-14: a disk-cleanup session read the stale row plus a 375 MB `du` and deleted `~/.cline/data` as "the gated agent's leftovers". Only `~/.cline/kanban` is legacy; `~/.cline/data/settings/{providers.json,models.json}` is live API-seat storage, so the delete took every seat and key with it and silently removed four UI surfaces (Cline in the agent picker, the Subagents row in create/inline/detail account pickers, Default subagent seat in Settings) plus broke resume on every Cline card. See [[cline-home-is-half-legacy-half-live]] in `backends/runtime/AGENTS.md` for the recovery sources. |
| 2026-08-31 | Agents tab: seat nodes, curated palette, full theme pass | Runtime + UI + flowise fork | The three "seat-wired" LLM routes were documented and reported **ready** by the Agents sidebar while **none of them could reach the proxy**, for two unrelated reasons. Flowise's own SSRF guard (`packages/components/src/httpSecurity.ts`) denies `127.0.0.0/8` + `localhost` with `HTTP_SECURITY_CHECK` defaulting **on**, and both `ChatGoogleGenerativeAI` and `ChatOpenAICustom` call `checkDenyList(baseUrl)` — so a node pointed at `http://127.0.0.1:3484/api/flowise-llm-proxy/*` threw `Access to this host is denied by policy.` before sending anything; upstream `ChatAnthropic` meanwhile has **no base-URL input at all**, so it could not be retargeted in the first place. Fixes: a fork-side `HTTP_ALLOW_LIST` (exact `host:port`, checked before the deny list) that the supervisor sets to the runtime origin only — *not* `HTTP_SECURITY_CHECK=false`, which would unguard every HTTP/Cheerio/Puppeteer/MCP node on a canvas that has no login; three credential-free nodes in a new **PixelOffice** palette category reading their base URL from the `PIXELOFFICE_FLOWISE_LLM_PROXY_*` env the supervisor already exported (they throw when it is absent rather than falling back to the public endpoint); and a `Base URL` input on upstream `ChatAnthropic`, mapped to `clientOptions.baseURL` (the shape `nodes/agentflow/Agent/Agent.ts` already sets for beta headers). The Anthropic route also now sends `anthropic-beta: oauth-2025-04-20` — the seat credential is a Claude Code OAuth bearer and neither the switchboard nor Flowise adds it. Two behaviour bugs found on the way: `resolveFlowiseLlmProxyStatus` reached `useManagerAccount()` through the gemini seat resolver, so **opening the Agents tab swapped the machine's global active seat** (the sidebar repolls every 5 s) — the status path is now a token-less `resolveFlowiseLlmGeminiSeatSummary` and `ResolveFlowiseLlmProxyStatusInput` no longer carries `useManagerAccount` at all, so the invariant is structural; and the Cursor route let any OmniRoute seat outrank a pinned Cursor seat while `rewriteOpenAiModelBody` overwrote the node's model on **every** request, now `ensureOpenAiModelBody` (fills a missing model only). Status stopped guessing: `available` still means "a seat exists", and a new optional `pathVerified`/`pathDetail` carries a **zero-token** models-listing probe (60 s cache), so the sidebar can say `seat · route failing` with upstream's own message. Palette curation is an allowlist at load time — `ENABLED_NODE_CATEGORIES`/`ENABLED_NODES` in `packages/server/src/utils/nodeFilter.ts` alongside upstream's `DISABLED_NODES`, driven from `backends/runtime/config/flowise/node-allowlist.json`; **fail-open** (missing/malformed = all ~280 nodes), because an unreadable list must not hide a node a saved flow depends on. Only the pool needs filtering: `/api/v1/nodes`, `AddNodes.jsx` and the AgentFlow v2 generator all read the already-filtered `nodesPool.componentNodes`. Theme: `palette.js` branched every unconditional colour on `isDarkMode` (`primary.main` was MUI **#2196f3** even in dark mode, status colours were MUI's) plus canvas/controls/edge CSS in the two `index.css` files behind a `pixeloffice-dark` class, since edge buttons and handles are shared with the light theme. Editors keep vscodeDark/sublime deliberately — `EditorView.baseTheme` loses to the theme extension, so overriding surfaces there only desyncs them. |

| 2026-09-03 | Run scripts follow npm conventions | Root scripts + installer + docs | `start` used to launch the *dev* stack while the production entry was the oddly named `solo`, `dev` duplicated `start`, and there was no `build` at all — so the documented install path ended with a manual `pnpm run solo`. Now: `setup` (interactive installer), `build` (web UI), `start` (one process, one URL — was `solo`), `dev` (Vite HMR + runtime), `restart`, `upgrade` (was `update-tenant`). Old names survive as aliases through `scripts/run-alias.mjs`, which prints one deprecation line and forwards, because ~35 files and every published doc referenced them. The installer no longer just prints a next step: it asks `Start PIXTiel now? [Y/n]` on a TTY and execs `pm.mjs run start`, with `--start` / `--no-start` to force either way and non-TTY runs still exiting 0 after printing the command (CI must not block on a server). Windows launchers call `npm start`, not pnpm — those installs guarantee Node, not corepack. |

| 2026-09-03 | Docs tab frames the product website | Runtime + UI | The **Docs** button used to open the doc-skill project browser (`DocsView`, sidebar + run panel + per-project iframe on the `:8323` sidecar); that surface is being re-homed to its own feature, so the button now frames `frontends/pixtiel-site` — the same Astro build that ships publicly — opened at `/docs/getting-started`. Served **root-mounted on its own port (3030)** by `src/site/site-server.ts`, an in-process `node:http` static server rather than a spawned sidecar (static files buy nothing from a supervisor, a restart policy and a pidfile). Root-mounted and not a path on :3484 because Astro emits absolute internal URLs (`/docs/…`, `/_astro/…`): a path mount needs every link rewritten through `import.meta.env.BASE_URL`, *including the ones inside MDX content*, which are plain strings — so a port keeps the published and embedded builds byte-identical. Gated on the build existing (`findSiteDistDir()`), with `site.status` reporting built/online separately so the tab can tell "never built" (prints `pnpm --filter pixtiel-site build`) from "built but nothing listening" (prints `--restart`). Two traps found while wiring it: `frame-ancestors` must come from `getKanbanRuntimePort()`, not `readBrandEnv("PORT")` — `--port` is a CLI flag, so the env default emits a list for :3484 while the board runs elsewhere and the frame is blocked with nothing in any log; and the bind host follows `KANBAN_RUNTIME_HOST` when set, because a frame at `127.0.0.1` viewed from another machine resolves to the *viewer's* loopback. `DocsView` and its doc-skill deps are left intact but unreferenced, awaiting their new home. |

| 2026-09-04 | Graph rebuild is observable | Runtime + UI | The Understand tab's **Rebuild graph** button worked all along — a measured run inlined the `understand` SKILL.md, ran the skill's own `scan-project.mjs` / `compute-batches.mjs` / `merge-batch-graphs.py`, detected 552 changed files and wrote a 25 MB graph — but nothing about a run was visible, so it read as a dead button, and the seat meter stayed flat. Three separate causes, none of them the skill. (1) `agy`'s stream reports every tool step as a bare `step_type` with **no** command and no output, and the planner's prose arrives in one record at the *end* of the turn: four minutes of identical `run_command (ACTIVE)` frames, then all 54 phase lines at once, which the UI further collapsed into a single overwriting string. The real detail is in agy's brain transcript (`~/.gemini/antigravity-cli/brain/<conversation_id>/…/transcript.jsonl`), keyed by a `conversation_id` the runtime already receives as the `session` meta — so `file-line-follower.ts` + `agy-brain-transcript.ts` tail it and forward each record as a `progress_line`. (2) A spawned agy collects **zero bytes on stderr**: it redirects its own fd 1 and fd 2 into `~/.gemini/antigravity-cli/log/cli-<ts>.log` while still writing stream-json to the pipe, so every auth failure and quota refusal has always been invisible. `--log-file` moves that log somewhere followable (verified not to disturb the stream), and `agy-log-file.ts` classifies it — with an explicit benign list, because a *successful* run logs a Playwright 404, a relative `.gemini`, indexer skips and `context canceled` teardown at `E` severity, and a bare `quota` pattern matches the `quotaProject=` every healthy auth prints. (3) Jobs are keyed by `projectPath` and a SIGSTOPped one was returned to every later request forever — `pause()` clears the idle watchdog, so only the 3-hour hard timeout ended it. Now: dead-child detection (`process.kill(pid, 0)`), a `rebuild_attached` frame telling the client whether it started or *joined* a job, and a `force` restart. Also names the account actually billed: `resolveManagerAccountPin` returns no env for `gemini` on purpose (those credentials are machine-wide, the pin only refuses runs), so a build bills whatever the keyring holds — parsed out of agy's log and shown, which is the whole answer to "it ran but my seat shows no usage". Build modes (`--full`, `--review`, the separate `understand-diff` skill) were considered and deliberately left out; the existing run is already the incremental/update mode. |

| 2026-09-04 | Merge conflicts are resolvable in-app | Runtime + UI | The top bar's **Resolve merge conflicts** button had never been able to show a conflict, and the only route through one was to leave the app: switch branch by hand, `git stash push`, resolve in a terminal, `git stash pop`. Five independent defects, any one of which alone made it dead. (1) Every integration action ran its own `--abort` in the failure branch — `merge --abort` for merge-to-base and merge-into-current, `rebase --abort`, `cherry-pick --abort` — so `MERGE_HEAD` never survived the request and `getMergeConflicts` (`git diff --diff-filter=U`) was correct to return nothing. `settleFailedGitOperation` now aborts **only** when the probe finds no unmerged paths, so a bad ref or a hook refusal is still cleaned up while a real conflict stands; *merge-into-current is deliberately left in the old shape* per the scoping call, and enabling it is the same three lines. (2) Merge-to-base with no worktree holding the base merged inside an `mkdtemp` worktree that its own `finally` deleted, so the conflict was erased before the response was even sent — replaced by `git-conflict-worktree.ts`, a stable checkout under `~/.agent/merge-worktrees/<repo>-<digest>/<ref>-<digest>` released only once nothing is unfinished there and *reused* by the next attempt (git 2.34.1 here, so `merge-tree --write-tree` is still not an option). (3) `runtime-config-query.ts` called `getMergeConflicts.query(null)` while accepting a `workspaceId`, so the dialog always read the home repo even though `resolveGitOpCwd` already supported task scoping; a new `getConflictState` now enumerates every worktree of the repo, and a client-supplied `worktreePath` is honoured only on an exact match against `git worktree list`. (4) `App.tsx` passed `onGitConflicts={selectedCard ? undefined : …}`, hiding the button on exactly the card whose worktree held the conflict. (5) The dialog offered only whole-file ours/theirs while the runtime's `side: "manual"` + `content` path had no caller at all — it now reuses the Review tab's own `SplitDiffGrid`/`buildUnifiedDiffRows` for ours-vs-theirs plus an editable pane seeded from git's marker file, with Commit/Abort/Skip-commit in the footer. Dirty-tree refusals ("Commit, stash, or discard changes before merging") are replaced by git's own `--autostash` on merge and rebase; cherry-pick has no such flag, so its refusal stands. **Measured on git 2.34.1 and worth not re-deriving:** the three stopped states are mutually exclusive by pseudo-ref (rebase = `REBASE_HEAD` + `rebase-merge/` and *no* `MERGE_HEAD`; cherry-pick = `CHERRY_PICK_HEAD`; merge = `MERGE_HEAD`), and `--autostash` parks the user's work in the `MERGE_AUTOSTASH` **ref** — not in `git stash list`, so looking there finds nothing and reads as lost work — restored by `commit --no-edit`, `merge --continue` and `merge --abort` alike. A conflicted file ships four texts, so past `CONFLICT_FILE_MAX_BYTES` (or a NUL in the first 8 KB) **all four** are nulled with `contentOmitted`, never one truncated side, which would diff against a full one and render a *wrong* diff. One existing integration test had to be inverted rather than extended: it asserted the base was untouched and the throwaway worktree gone, i.e. it pinned the exact behaviour that made the button useless. |

| 2026-09-05 | Review chat: terse answers, symbol locator, rtk | Runtime + UI | The panel answered in prose that weighed both sides and narrated the assistant revising its own earlier draft, verdict several paragraphs down — `REVIEW_CHAT_SYSTEM_PROMPT` constrains what it may *say* and never its form, and the machine-wide caveman `SessionStart` hook cannot reach a spawn that pins `CLAUDE_CONFIG_DIR` to a Manager seat. A **Terse** toggle (panel header, default on, localStorage like the comment-polish switch beside it) sends `terse` per turn and the runtime appends `review-answer-style.ts` to `--append-system-prompt`; the `suggestions` fence's `message`/`reasoning`, security findings and ordered instructions are carved out, because those are published to the merge request's author rather than read in this panel. **Understand Anything was already used here** — `review-graph.ts` walks the graph in TypeScript and the agent is forbidden the 24 MB file — so the addition is a *locator*, not a describer, and the distinction is measured rather than stylistic: a node carries no `signature`, `params` or `returns` in either graph on disk, and `akselos-dev`'s 33 511 nodes carry **zero** `lineRange` and **zero** `calls` edges with 24 530 template summaries, so anything promising types would be a no-op there and a confident non-answer here. Knowing `get_empirical_data_dir` resolves to two files is what turns a hedge into one targeted `Read`. Extraction runs before the graph loads (a prompt naming no symbol never pays the parse) and is never fed the diff or the selection — every identifier in a hunk hits the index. Delivered as a pre-resolved prompt section rather than a CLI the agent shells out to: with no MCP in this spawn the only lever is `Bash(...)`, and a fresh node process per question re-parses 24 MB with no shared cache, which is the grep cost `review-graph.ts` exists to remove. `rtk` finally becomes callable — it was on PATH via `withStackBinOnPath` all along but denied by the tool allowlist — as **seven enumerated subcommands**, never `Bash(rtk:*)`: `rtk run` is `sh -c` and `rtk proxy` is unfiltered exec, so the wildcard would be arbitrary command execution past an allowlist whose whole point is that this panel only reads. See [[the-knowledge-graph-cannot-answer-a-type-question]], [[rtk-in-an-allowlist-must-be-enumerated]] and [[caveman-cannot-reach-a-seat-pinned-spawn]] in `backends/runtime/AGENTS.md`. |

| 2026-09-05 | Review tab: collapsible chrome + overview ruler | UI | The Review surface spent its space on chrome: two hard-coded asides (`w-80` tabs, `w-96` Claude panel) that could be neither resized nor collapsed, and `ReviewTagStrip` between the file toolbar and the first diff line — ~90 chips open, three header rows even fully closed. Both asides now collapse to a 32px `ReviewSideRail` of their own tabs — `use-review-workspace-layout.ts` holds only that one axis (two persisted booleans, both in `LAYOUT_CUSTOMIZATION_LOCAL_STORAGE_KEYS` so "reset layout" clears them), since the same-day `useReviewLayout` already owns every draggable size in the tab; it gained optional `isSidebarCollapsed`/`isClaudePanelCollapsed` so a collapsed column reserves its rail rather than its stored width, or collapsing one aside frees space the other refuses to grow into — a rail rather than nothing, because the file list is navigated constantly and the collapsed Impact icon is the only stale-graph signal outside the tab. The tag palette moved to a rail down the diff's left edge (`review-tag-rail.tsx`, palette extracted intact to `review-tag-palette.tsx`) whose flyout **auto-hides on `dragstart`** unless pinned: a flyout wide enough for 66 chips covers the rows the chip has to land on. New: an overview ruler (`review-diff-minimap.tsx` + pure `review/review-minimap.ts`) painting added/removed runs, annotation dots in the tag's own colour, a viewport thumb and click/drag-to-scroll, plus a whole-MR file strip (`review-file-rail.tsx` + `review/review-file-bands.ts`) since the pane only ever mounts one file. Three things the implementation turns on: the ruler measures rows through `getBoundingClientRect` and never `offsetTop` — the scroller is unpositioned, and giving it `position: relative` would re-stack the composer and the flyout over it; rows gained `data-diff-variant` / `data-annotation-kind` / `data-annotation-color` so the ruler reads geometry instead of re-deriving the diff, and split mode's duplicate row keys are merged keeping the annotated side; and the re-measure effect is keyed on a memoised `minimapRevision`, since a fresh object each render loops against the measure pass's own state. jsdom has no `ResizeObserver`, so that observer is feature-detected (same guard as `dependency-overlay.tsx`). |

## Commit & PR message style
- Do not add `Co-Authored-By: Claude ...` trailer to commits in this repo.
- Do not add "🤖 Generated with Claude Code" (or any AI-attribution) line to PR descriptions in this repo.
- Everything else (Conventional Commits format, PR Summary/Test plan structure) stays as-is.
