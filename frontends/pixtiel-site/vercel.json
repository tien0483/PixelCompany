{
	"framework": "nextjs",
	"installCommand": "cd ../.. && pnpm install --frozen-lockfile --filter pixtiel-site...",
	"buildCommand": "pnpm build",
	"ignoreCommand": "git diff --quiet HEAD^ HEAD -- ."
}
